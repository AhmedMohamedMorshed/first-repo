/*
 * ex2.c
 *
 *  Created on: Feb 1, 2023
 *      Author: MEGA
 */
/*

#include <stdio.h>

int main(void)
{
	int n,i,sum=0;
	float arr[100];
	printf("enter number of data\n");
	fflush(stdout); fflush(stdin);
	scanf("%d",&n);
	if(n>100)
	{
		printf("number of data must less or equal 100\n");
	}
	else
	{
		for(i=0;i<n;i++)
		{
			printf("enter element a %d\n",i+1);
			fflush(stdout); fflush(stdin);
			scanf("%f",&arr[i]);
			sum+=arr[i];
		}
		float avg=sum/n;
		printf("average of data = %f\n",avg);

	}
}
*/
